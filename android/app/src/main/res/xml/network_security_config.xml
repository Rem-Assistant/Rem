<?xml version="1.0" encoding="utf-8"?>
<!-- Dogfood-only: allow cleartext so local Mac/LAN gateways can be reached while testing. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
